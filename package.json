{
  "name": "terraform-playground",
  "version": "1.0.0",
  "description": "This is a Terraform setup running with this configuration: - State is stored in [tfstate.dev](https://tfstate.dev) - Plans and applies run in GitHub Actions - State is backed up to Backblaze B2",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  }
}
